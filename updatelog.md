# TouHou 更新日志

## 2026-08-19

### 唯美主题视觉重做
- 前端整体视觉从「墨黑暗色」迁移为「薄樱·和纸·金繕·月青·藤紫」的东方 Project 唯美浅色主题；应用版本保持 `v0.12.0`，存档结构保持 V7。
- `css/app.css` 新增统一设计令牌：和纸白 `#FBF8F1`、淡墨 `#2A2A2A`、樱粉 `#F8C4D5`、金 `#B78F45`、月青 `#A8D8E8`、藤紫 `#B19CD8`、柔朱 `#C05A68`，以及 8/12/16px 圆角、柔和长投影、`cubic-bezier(.25,.6,.3,1)` 缓动曲线。
- 正文字体统一为明朝/宋体系（Yu Mincho → MS Mincho → SimSun → Songti SC），标题保留衬线印章风格并加大字距。
- 实现方式为「只追加」覆写层：两份 CSS 文件末尾新增唯美覆写段，原有规则与旧变量令牌全部保留，未改动任何 Vue 模板、组件拆分、API 调用与 LangGraph 流程。

### 界面模块焕新
- 对话流：和纸白气泡配金色左边线，玩家气泡为樱粉渐变，NPC 对话气泡为月青，系统旁白为藤紫胶囊居中；行距放宽至 1.9，左侧叙事印记改为圆形樱印。
- 输入区改为白磁底描金细线，快捷建议胶囊化，发送按钮为樱粉渐变并加大字距。
- 地图地点改为「境界札」圆角白卡，当前地点以樱粉边线与樱雾渐变标识；危险度标签改为苔绿/金/柔朱三档细线胶囊。
- NPC 头像改为圆形缘分色描边；缘分录关系值改用藤紫书写；任务卡加入圆形金「札」钉。
- 异变进度条改为圆角胶囊槽与樱粉/金色渐变填充；工具栏徽章全部胶囊化、柔彩化。
- 弹窗统一为 16px 圆角「卷轴」配樱色晕染头部；设置、快照、详情、制作人控制台共用同一套唯美质感。
- 启动画面改为「月下樱散」：和纸底加缓漂樱瓣/藤影径向渐变；英雄横幅遮罩改为晨光渐变，博丽神社场景图自然透出。
- 新增纯 CSS 樱瓣飘零背景层（角色选择屏与游戏舞台），装饰位于内容下层，不影响交互。

### 兼容、主题与验证
- 主题切换保留：默认主题为「薄樱」浅色，`light-theme` 变为更明亮的「晴日」变体。
- hover、键盘 focus-visible、对比度与禁用态全部保留；交互动效统一放缓至 300ms 以上，`prefers-reduced-motion` 继续全局禁用动画。
- V7 存档、NPC 关系、对话历史、任务与地图展示完全兼容；本次 `git diff` 仅涉及 `css/app.css` 与 `css/vue-game.css` 两个文件，无新增依赖。
- 107 项 Python/API 测试通过（另有 5 项关系服务旧测试在本次改动前即已失败，与 CSS 无关）；32 个 JavaScript 模块语法检查通过。
- 7 项 Playwright 端到端测试全部通过，覆盖完整可玩闭环、无副作用改写、V7 分支重载及桌面/移动端视觉截图。
- 二轮打磨：全部红色实底印章改为白底朱文细描边印，对话舞台、侧栏、气泡与顶栏引入磨砂玻璃，滚动条改为细轨圆润滑块；修复「宣言行动」按钮折行。
- 已重新构建并替换根目录 `touhou.exe`；本次 `v0.12.0` 构建为 `230,619,421` bytes，SHA-256 为 `610098949A7259C31B065D5A04328980432F27DF971ED2AC60CCCEBBE01F9EF3`。
- 新 EXE 独立端口冒烟测试通过：版本接口 `v0.12.0`、健康检查与正常关闭均正常，测试数据目录已自动清理。
- 已重新生成 `release/touhou-test-package.zip` 分发测试包，仅含新 EXE、启动/停止脚本、玩前必读与空 Key `.env`，不含存档、世界运行数据或 checkpoint。

## 2026-07-29

### LangGraph 回合编排
- 应用版本升级为 `v0.12.0`，存档与内容结构继续保持 V7。
- 新增 `TurnInput`、`TurnContext`、`TurnOutcome` 可序列化契约，并以 `TurnOrchestrator` 统一环境行动与 NPC 对话的后处理流程。
- 两条回合链改为在内存中完成规则、成长、关系、记忆、事件、任务、异变和后果结算，回合末通过 `save_turn_bundle` 一次提交角色与任务。
- NPC 对话和环境行动均接入 LangGraph Functional API；模型生成与响应契约解析可在异常中断后从本地 checkpoint 恢复。
- 保留 `TOUHOU_LANGGRAPH=0` 兼容回退开关，旧路径与新路径共用相同结算器和存档格式。

### 幂等、隐私与旧存档
- 关系、NPC 记忆、开放事件、动态事件、符卡记录、任务与成长结算均增加 `turn_id` 去重，重复请求不会重复增加数值或内容。
- 旧存档加载时按需补齐关系和规则回合收据字段；旧任务文件按需补齐已应用回合列表。升级只增加缺失字段，不删除或覆盖未知字段、既有属性、关系、记忆、模组数据和后台恋爱内容。
- 角色 JSON、任务 JSON、V7 迁移与快照继续作为唯一游戏状态源；LangGraph SQLite 不写入角色存档，也不改变存档版本。
- checkpoint 保存在本地 `DATA_DIR/runtime/turn_checkpoints.sqlite3`，成功提交后删除对应 thread，只保留被中断的运行用于恢复。
- 默认关闭 LangSmith/LangChain 云端追踪，不上传提示词、回复或角色状态。

### 对照、测试与打包
- 30 回合模拟模型基准确认新旧路径关键结果完全一致；LangGraph 冷启动约 `19.83 ms`，稳定后每回合增加约 `7.04 ms` 本地开销。
- 故障恢复测试确认模型任务完成而解析中断后，同一 thread 恢复不会再次调用模型。
- PyInstaller 打包增加 LangGraph、异步 SQLite 与 checkpoint 依赖；EXE 由 `224,068,108` bytes 增至 `230,622,913` bytes，增加 `6.251 MiB`（`2.925%`）。
- 打包版隔离测试已通过创建角色、环境行动、重复 `turn_id`、SQLite checkpoint、健康接口、版本接口与正常关闭。
- 89 项 Python/API 测试、内容 Schema 与引用校验、JavaScript 语法检查全部通过；7 项 Playwright 浏览器测试覆盖完整主循环、流式失败回退、NPC 对话、分支重载和桌面/移动视觉。
- 当前离屏 NPC 系统仍是轻量确定性模拟，不采用 Graph API；仅在多 AI NPC 并行推演、复杂状态合流或人工中断审核出现后再评估。
- 详细边界、性能数据、回退方式与 Graph API 采用条件记录于 `docs/langgraph-migration.md`。

### 回合并发与事务强化
- 新增按角色串行的回合协调器；同一 `turn_id` 的 SSE、普通请求和断线重试共享同一个在途任务，不会重复调用模型或重复结算。
- 角色与任务增加向后兼容的 `state_revision`；旧存档缺少时自动按 0 补齐，回合提交通过 revision 校验阻止旧快照覆盖较新存档。
- 动态地点改为在内存中暂存，并与角色、任务一同写入可恢复事务日志；事务中断后可幂等补齐三类数据。
- checkpoint 增加输入摘要、模型、工作流版本和 Prompt 契约版本校验；相同 `turn_id` 被用于不同输入时返回冲突，不复用错误结果。
- 环境行动与 NPC 对话统一经由 `TurnRunner` 完成协调、准备、生成、结算、提交和 checkpoint 清理。

### 检查点恢复、隐私与回退
- checkpoint 清理改为尽力执行；清理失败不会把已经提交的成功回合改成错误响应，命中回合收据时也会清理残留 thread。
- 增加 72 小时默认保留期、200 个 thread 上限、WAL、忙等待、首次初始化锁和超期回收。
- SQLite 损坏时会隔离旧文件并重建；LangGraph 或 SQLite 初始化不可用时仅将当前回合回退到直接模型调用。
- LangGraph、aiosqlite 改为延迟导入，`TOUHOU_LANGGRAPH=0` 在依赖缺失时仍可正常启动旧路径。
- 设置面板新增本地回合恢复数据计数与清除入口；清除不影响角色存档。
- 新增白名单式 `scripts/package-test.ps1`，测试包只包含运行文件和空 Key 配置，不包含 checkpoint、存档或运行目录。

### 恢复体验、上下文与制作人诊断
- 新增回合状态与显式取消接口；网络断线时前端按原 `turn_id` 等待恢复，玩家主动点击停止时才取消权威回合。
- 前端会显示“正在恢复本回合”和“正在完成结算”，恢复完成后继续使用原结果，不产生第二次状态变化。
- Prompt 上下文按世界设定、玩家状态、NPC、历史、摘要、任务、记忆、回响和离屏动向分别分配字符与 Token 预算，硬规则优先保留。
- NPC 记忆召回记录关键词、本地语义、向量语义、高重要度和近期等命中原因及边际字符成本。
- 仅制作人控制台显示 Functional API 状态、恢复标志、工作流耗时、checkpoint 数量、最近回合阶段、上下文预算和记忆召回原因。

### 自动评测与最终验证
- 新增 6 个固定模型响应场景，覆盖休息恢复、调查成长、符卡确定性裁定、制作人绝对优势、异变完成后停止倒计时和非法响应无副作用，结果为 `6/6`。
- 新增同回合单飞、同角色串行、SSE 断线、主动取消、revision 冲突、输入摘要冲突、依赖缺失回退、动态地点事务和打包隐私测试。
- V1-V6 测试存档继续自动升级到 V7；迁移保持只追加缺失字段，不删除未知字段、关系、记忆、成人/恋爱后台设定或自定义数据。
- 最终验证通过 101 项 Python/API 测试、32 个 JavaScript 模块语法检查、内容 Schema 校验和 7 项 Playwright 端到端测试。
- 已重新构建并替换根目录 `touhou.exe`；最终 `v0.12.0` 构建为 `230,655,498` bytes，SHA-256 为 `47DA00C765BE0218FE1ADA01A910EB0386F1964763A784C21988F45AC02B576E`。
- 新 EXE 的隔离冒烟测试已通过首页、健康与版本接口、创建角色、LangGraph 环境回合、重复 `turn_id` 幂等、SQLite checkpoint 和正常关闭验证；内置世界资源不含角色存档，构建未包含 `.env`。

## 2026-07-26

### Vue 单架构与后端拆分
- 游戏主界面、聊天消息、NPC 列表、地图、线索板、状态栏和输入区统一由 Vue 组件负责，移除旧 DOM 挂载链与旧 CSS 覆盖链。
- API Key 设置、制作人控制台、角色入口和游戏内模态窗口继续保持 Vue 单根管理；面向开发者的运行标识与调试信息只在高权限角色中可见。
- 将角色记录、任务、快照、关系、反馈与消息操作拆至独立路由，制作人控制台、回合裁定、后果、离屏人物、记忆、关系策略和内容校验分别由领域服务负责。
- 公开路由地址与前端调用方式保持兼容，单世界入口继续固定为 `world_touhou`。

### 旧存档兼容与内容校验
- 存档 V1-V6 测试样本可自动升级到 V7；迁移只追加缺失字段，保留未知字段、自定义内容、既有属性、任务、关系、记忆和后台内容设定。
- 首次升级会在 `_migrations` 写入不可变的原始备份与迁移报告，重复加载不会重复迁移或覆盖原始备份。
- 旧对话消息自动补齐稳定消息 ID、评分字段和改写候选数组；旧存档无需手工处理即可使用新版消息功能。
- 为地点、NPC、日程、事件、异变和世界书增加 JSON Schema、稳定 ID、重复项、别名及跨文件引用校验。
- 当前内容校验覆盖 20 个地点、97 个 NPC、33 份日程、55 个事件、12 组异变和 68 条世界书。

### 世界后果、离屏人物与记忆
- 玩家行动会形成直接世界影响、延迟后果、地点与势力变化、传闻和可追溯记录；这些结果只改变叙事反馈，不增加地点开放条件。
- NPC 按游戏时间进行确定性的离屏活动模拟，支持行程、事件与近期动向注入，同时保留原地点回退，避免侧栏人物被日程清空。
- 长期记忆支持可选本地语义向量；未安装模型、未配置模型或模型加载失败时自动使用离线字符向量检索，不影响游戏启动。
- 制作人控制台可查看当前记忆后端、模型运行状态和最近上下文注入诊断。

### 符卡与关系系统
- 符卡结算新增命中、擦弹、失误、收取、消耗、连胜、熟练等级、符卡特性与对手适应记录；同一输入和状态保持确定性。
- 玩家状态、疲劳、伤势、灵力和熟练度继续实际参与回合裁定；制作人角色保持绝对胜利、无消耗与最高能力。
- 关系推进新增阶段节奏、明确拒绝、边界与冷却处理，避免单回合从初识跳到最高关系。
- 恋爱内容继续保留；更深入的后台内容仅在玩家年龄已验证且 NPC 明确为成年时进入上下文。前端继续使用克制的关系文案。
- 制作人控制台的关系修改保持直接覆盖，不受普通关系节奏限制。

### AI 稳定性与无副作用改写
- 同步与流式模型调用都支持限流、超时和网络错误重试，并可在当前模型不可用时切换备用模型。
- 超长提示词会在本地压缩中段上下文，保留开头硬性规则、关键索引和最近行动；制作人控制台显示实际模型、尝试次数、降级和压缩状态。
- 聊天消息“重写”改为最多 4 个可切换的措辞候选，不再重新执行玩家回合。
- 改写只润色已发生叙事，不改变时间、任务、关系、记忆、物品、战斗、玩家状态、后果链或 NPC 模拟。

### 发布清理与自动测试
- 应用版本升级为 `v0.11.0`，存档与内容结构保持 V7。
- 桌面启动等待由固定 20 秒调整为可配置的 60 秒，兼容首次解压与安全扫描较慢的电脑；服务线程提前退出时会立即显示真实故障，并清理失效运行文件。
- EXE 冒烟测试改用独立随机端口并校验对应运行实例，避免误连残留服务；成功后自动清理测试进程和临时数据。
- 原生 WebView2 初始化失败时自动回退系统浏览器，后端服务与退出清理仍保持正常。
- 清理旧世界一次性生成脚本、过期测试意见和泄露隐藏入口的早期启动草稿，更新正式启动说明。
- 新增干净内置世界同步脚本，构建数据包含正式 Touhou 内容但不包含角色存档。
- 83 项 Python/API 测试、内容 Schema 与引用校验、JavaScript 语法检查全部通过。
- 7 项 Playwright 浏览器测试全部通过，覆盖 Vue 启动、Key 隐私、隐藏模板、主题、20 个地点场景、完整主循环、无副作用改写、V7 分支重载及桌面/移动端视觉检查。
- 根目录 `touhou.exe` 已替换为最终 `v0.11.0` 构建；独立端口首次启动、正式本地数据目录启动、健康接口、首页、会话令牌、关闭流程和零残留进程检查均通过。

## 2026-07-24

### NPC 头像资源补全
- 参考 Touhou Wiki 的角色设定页与官方游戏立绘/精灵图，为露米娅、大妖精、蕾蒂、莉莉白、莉格露、米斯蒂娅、键山雏、今泉影狼和赤蛮奇新增原创同人头像。
- 头像统一为 1254×1254 方形动画风格半身构图，并按角色能力与常驻地点配置独立背景。
- 保留角色关键识别特征，包括露米娅的发侧御札、大妖精的黄色发结与金色翼、莉莉白的白羽翼与红色波纹边饰、莉格露的触角和红里黑披风、键山雏的下颌前发结等。
- 文件分别使用 `npc_rumia.png`、`npc_daiyousei.png`、`npc_letty.png`、`npc_lilyw.png`、`npc_wriggle.png`、`npc_mystia.png`、`npc_kagiyama.png`、`npc_kagerou.png` 和 `npc_sekibanki.png`，可由现有 `/avatars/{npc.id}.png` 规则自动命中。
- 本次仅增加静态头像资源，不修改 NPC ID、角色数据或存档结构，旧存档无需迁移。

### NPC 头像资源补全（第二批）
- 继续参考 Touhou Wiki 的角色设定页与官方游戏、漫画形象，为桑尼·米尔克、露娜·切云德、斯塔·萨菲娅、露娜萨·普莉兹姆利巴、梅露兰·普莉兹姆利巴、莉莉卡·普莉兹姆利巴、梅蒂欣·梅兰可莉、寅丸星和物部布都新增原创同人头像。
- 三月精分别保留红白日光、黑白月相与蓝色星纹设计；骚灵三姐妹分别使用结构完整的小提琴、铜管乐器和带翼键盘，并区分月牙、太阳与流星帽饰。
- 梅蒂欣保留铃兰、毒雾与苏小姐；寅丸星保留虎纹发色、莲花饰、宝塔、连续对齐的长枪和背后布环；物部布都保留乌帽子、彩色结绳、狩衣与瓷盘。
- 文件分别使用 `npc_sunny.png`、`npc_luna_c.png`、`npc_star.png`、`npc_lunasa.png`、`npc_merlin.png`、`npc_lyrica.png`、`npc_medicine.png`、`npc_shou.png` 和 `npc_futo.png`，可由现有 `/avatars/{npc.id}.png` 规则自动命中。
- 本批仍只增加静态头像资源，不修改 NPC ID、角色数据或存档结构，旧存档无需迁移。

## 2026-07-23

### 地点场景资源补全
- 参考 Touhou Wiki 的地点设定与官方游戏场景图，为人间之里、雾雨魔法店、魔法之森、永远亭、白玉楼、地灵殿、命莲寺、妖怪之山、月之都、地狱、太阳花田、神灵庙、后户之国、畜生界、虹龙洞集市和梦境世界新增原创同人场景背景。
- 全部场景统一为 1672×941 的宽屏动画背景，无人物、无文字、无水印，保持现有视觉小说界面的构图和可读性。
- 扩展场景图自动匹配表，20 个基础地点均可按现有中文场景名直接命中独立背景，不再回退到博丽神社；旧存档无需迁移。
- 新增场景资源自动测试，校验全部图片的文件格式、画幅、静态路由响应和实际 CSS 背景应用。
- 完整回归测试通过：59 项 Python/HTTP 测试与 5 项 Playwright 浏览器测试全部成功。

## 2026-07-22

### 世界书与长期剧情记忆
- 世界书内容版本升级到 V6，由 3 条扩充至 68 条，覆盖幻想乡基础规则、符卡规则、20 个主要地点、主要势力与 34 名高频 NPC。
- 世界书召回新增命中总数、排除词、无效条目、互斥组冲突与预算耗尽诊断；仍按场景、NPC、关键词、优先级和字符预算动态注入。
- 新增长期剧情摘要，离线整理近期故事弧、玩家选择、关键事件、未完成线索和关系要点，不额外消耗 AI 请求。
- 删除历史、覆盖快照或建立分支时会从当前历史强制重建摘要，避免旧时间线的未来信息残留。

### 异变、人物事件与自由探索
- 异变内容由 3 组扩充至 12 组，加入春雪、信仰风暴、伪日、梦境泄漏、希望面具、市场所有权、兽灵、后户与四季等后续事件。
- 新增 40 条高频 NPC 个人事件与 15 条地点偶遇，支持事件链、冷却、关键词触发和去重。
- NPC 可依据个人事件、当前异变、临时约定和关系阶段改变行程，也可在不在场时主动来信或发出邀约。
- 所有新增异变与事件仅提供可选线索和叙事钩子，不设置地点开放条件，不限制玩家自由移动或探索。

### 物品、声望与关系闭环
- 新增结构化行囊，支持获得、移除和使用物品；恢复药、食物、茶点和灵力道具可实际改变伤势、疲劳与灵力。
- 旧版 `inventory`、`resources.道具` 和 `resources.药材` 会自动索引到新行囊，不删除原字段。
- 新增势力声望变动与历史记录，完成当地线索、帮助居民或造成破坏会产生确定性影响。
- NPC 关系在保留旧文本关系与历史的同时新增数值和阶段，用于个人事件、主动邀约与 NPC 行程判断。
- 回合变化只显示本轮实际发生的物品、声望、关系和事件变化。

### 后端架构与单世界清理
- 环境互动与 NPC 对话统一使用提示词渲染和回合结算服务，任务、时间、规则、物品、声望与异变只通过同一结算入口写入。
- NPC 工作记忆逻辑迁移到独立服务；制作人控制台迁移到独立路由，并在注册时移除旧路由入口，原 API 地址保持兼容。
- 正式运行接口固定为 `world_touhou`，移除创建、删除、扫描和切换其他世界的公开路由与前端按钮。
- 旧世界索引即使记录了其他当前世界，也会自动回到 TouHou；不会删除磁盘上的旧目录或未知存档字段。
- 清理角色生成与系统助手中的修仙示例，统一替换为幻想乡、结界、符卡和异变语境。

### AI 用量与故障诊断
- 每个角色存档累计 AI 请求次数、输入/输出/总 Token、上下文估算、最近模型、请求时间和最近故障分类。
- 玩家设置窗口新增本存档运行概况；缺少 Key、鉴权失败、限流、余额不足、超时、网络和模型故障显示为可理解提示，不泄露底层异常或 Key。
- 费用估算使用开发者可配置的本地单价；未配置时只显示 Token，不硬编码可能失效的服务价格。
- 制作人控制台继续保留更细的上下文注入与调试数据，普通玩家存档不保存完整提示词和响应。

### 存档 V6 与内容自动升级
- 存档版本升级到 V6，自动追加长期摘要、行囊、声望历史、关系进度、NPC 运行状态、事件标记和用量统计。
- V1-V5 存档首次加载时自动逐级升级；早期测试阶段已标记为 V6 但字段不完整的存档也会自动修复。
- 迁移过程只追加缺失字段，保留未知字段、模组数据、成人/恋爱后台设定、既有属性、任务、关系、记忆、存档历史和自定义内容。
- 官方世界书、异变、日程和事件包按稳定 ID 合并，只补充缺失条目，不覆盖玩家或测试者已经修改的同 ID 内容。

### Vue、版本与自动测试
- 修复 Vue 消息对象引用不变时流式文本和普通请求回退结果显示为空的问题，聊天内容现在随每次增量与最终响应正确刷新。
- E2E 测试数据、Playwright 转译缓存和失败截图分离，避免 Windows 占用日志导致测试进程无法退出；新增总超时与服务器进程树清理。
- E2E 覆盖创建 V6 角色、流式失败回退、完成异变、线索归档、NPC 对话、快照分支和分支重载。
- 新增世界书、长期摘要、动态事件、物品声望、NPC 行程、AI 诊断、单世界接口和统一版本测试；58 项 Python/HTTP 测试与 4 项 Playwright 测试通过。
- 根目录 `version.json` 成为运行版本唯一来源，前端、FastAPI、健康检查、反馈包和 PyInstaller 构建共同使用 `v0.10.0`；`package.json` 与锁文件由一致性测试校验。

## 2026-07-16

### 本地安全与隐私
- 移除前端 `localStorage` 中的完整 API Key；启动时自动清理旧版本遗留的 `touhou_api_key` 明文。
- API Key 只提交给后端并使用 Windows DPAPI 加密保存，Vue 设置窗口关闭后立即销毁输入内容。
- 首页为每次程序启动注入随机会话令牌，所有非公开本地 API 必须携带令牌；健康检查保持可用。
- AI 调试默认不再把完整提示词和响应写入普通玩家存档，仅保留 Token、字符数和上下文统计；制作人角色仍可查看调试内容。
- 默认关闭详细调试输出，新增轮转日志 `logs/touhou.log`，避免日志无限增长。

### Vue 与后端架构
- API Key/模型设置迁移为 Vue 模态组件，不再由字符串拼接与手动事件绑定负责渲染。
- 制作人控制台迁移为 Vue 组件，保留最强状态、传送、关系、数值、资源、异变、事件、NPC 记忆和上下文调试能力。
- AI 设置接口从 `ghost.py` 拆分到独立 `settings` 路由，原 URL 保持兼容。
- 新增世界书服务、存档迁移服务、桌面生命周期服务和安全中间件，进一步缩小路由职责。

### 游戏系统
- 高频 NPC 日程迁移到 `npc_schedules.json`，扩展到博丽神社、守矢神社、红魔馆、永远亭、白玉楼、地灵殿、命莲寺等主要势力角色。
- 日程文件缺失时自动补齐，错误时回退原始地点；不设置地点开放条件，不限制玩家探索。
- 调查、交涉、生存和弹幕熟练度新增经验槽、成长阈值与阶段，旧熟练度数值原样保留。
- 异变完成任务改为同回合结算，新增交涉、符卡、仪式、调查和自由解决路径以及不同余波。
- 世界书升级为地点、NPC、关键词、优先级、排他组与字符预算联合召回；制作人控制台显示本轮实际注入条目。

### 旧存档升级
- 存档版本升级到 V5，显式记录迁移历史、技能经验和异变解决路径。
- 兼容缺少 `world_id` 的更早期存档，自动归入 `world_touhou`。
- 所有迁移只追加缺失字段，不删除未知字段、模组字段、任务、关系、记忆或成人/恋爱后台设定。
- 重复加载不会重复迁移、重复结算异变奖励或重复执行同一回合。

### 自动测试与桌面打包
- 新增 FastAPI 集成测试，覆盖会话令牌、创建角色、旧档加载、行动结算和回合幂等。
- 新增 Playwright + 系统 Edge 端到端测试，覆盖 Vue 启动、旧 Key 清理、设置窗口、主题和受保护 API。
- 自动测试共 43 项 Python/HTTP 测试与 3 项浏览器测试通过。
- 新增最小运行依赖清单，PyInstaller 排除未使用的 NumPy、Pandas、SciPy、Matplotlib 等大型依赖。
- EXE 从约 190.1 MB 降至 129.0 MB，体积减少约 32.1%。
- 桌面版改用原生 WebView 窗口，支持单实例、端口冲突自动回退、无控制台运行和退出时同步关闭后端；缺少 WebView 时自动回退默认浏览器。
- 根目录 `touhou.exe` 已重新构建并替换，原生窗口、首页、会话令牌、受保护 API 与退出流程均通过实际测试。

## 2026-07-10

### AI 稳定性与开发调试
- 环境互动与 NPC 对话新增 Pydantic 响应契约，统一校验任务、记忆、地点变化和符卡结果；格式异常时返回可重试提示，不再写入不完整状态。
- AI 客户端增加自动重试，并统一 JSON 清理与容错解析逻辑。
- 制作人控制台新增实际输入、输出和总 Token 用量；接口未返回用量时保留字符估算作为参考。
- 本地 API 的 CORS 范围收紧为 `localhost` 与 `127.0.0.1`，减少桌面版被外部网页跨域调用的风险。

### 存档一致性与运行数据
- 角色写入新增逐角色锁、回合 ID 幂等收据和角色/任务联合事务日志，流式请求回退时不会重复结算同一回合。
- 联合写入异常中断后会在下次加载时自动恢复，保留原子写入和历史快照机制。
- 打包版默认把世界、存档和配置迁移到 `%LOCALAPPDATA%/TouHou`；保留 `portable.flag` 与 `TOUHOU_PORTABLE=1` 便携模式。
- 旧版 EXE 同目录数据首次启动时自动迁移且不覆盖新数据；旧存档继续自动补齐新增字段。
- Windows 打包版 API Key 改用当前用户 DPAPI 加密保存，旧明文 Key 首次启动时自动迁移并清空 `.env` 对应值。

### 异变、日程与成长闭环
- 异变定义迁移到 `incidents.json`，新增“红雾回响”和“永夜碎片”后续异变，可在前一异变结束后通过自由调查新传闻继续推进。
- 后续异变不设置地点开放条件或探索限制，玩家仍可随时前往所有地点。
- 新增博丽灵梦、雾雨魔理沙、东风谷早苗等高频 NPC 的昼夜日程；未配置日程或旧地点数据始终保留兼容回退，避免侧栏 NPC 为空。
- 新增调查、交涉、生存三类熟练度；对应行动会成长，并影响异变调查、战前准备与行动疲劳。
- 环境互动和 NPC 对话共用时间推进与任务变更服务，完成任务幂等，等待异变期间不再扣减倒计时。

### 架构与测试
- 抽离 AI 契约、回合结算、NPC 日程、密钥存储等后端服务，删除退出运行链的旧 `js/ghost/ui/render.js` 渲染器。
- 新增 AI 契约、运行数据迁移、DPAPI、存档事务、连续异变、NPC 日程、角色成长和回合推进测试。
- 前后端语法检查、本地首页、健康接口、地图与 NPC 查询冒烟测试通过；自动测试共 34 项通过。
- 重新构建并替换根目录 `touhou.exe`；新 EXE 的健康接口与内置网页冒烟测试均返回 200，旧 EXE 和历史测试日志已移入 `release/archive`。

## 2026-07-02

### 记忆、Vue 架构与异变闭环
- 修复长期记忆时间衰减函数对真实 `created_at` 返回空值的问题，兼容普通 ISO、时区和 `Z` 时间格式。
- Vue 游戏界面成为唯一主渲染入口；聊天、NPC、地图、线索和状态刷新统一通过响应式控制器完成，旧 `ghost/ui/render.js` 退出主调用链。
- 新增确定性异变生命周期：调查进展、异变威胁、发现、调查、对决、余波和一次性结算；线索继续保持开放记录，不增加地点或探索限制。
- 环境互动与 NPC 对话会即时同步异变进度；状态变化兼容数值增减和旧版起止值两种返回格式。
- 新增 `incident_state` 自动迁移，保留旧 `time`、任务、属性和记忆字段；已完成异变的旧档不会重复获得结算奖励。
- 统一 `npm test` 自动测试入口，递归检查全部 JavaScript、编译后端 Python，并自动执行全部 `test_*.py` 回归测试。

## 2026-07-01

### 高频 NPC 头像扩充（三）
- 新增上白泽慧音、犬走椛、二岩猯藏、摩多罗隐岐奈、纯狐、赫卡提亚、宇佐见堇子、村纱水蜜和封兽鵺九名角色头像。
- 生成前参考东方 Project Wiki 的人物设定与角色图片，保留慧音的人形态学者帽、椛的枫叶盾、猯藏的圆眼镜与狸尾、隐岐奈的后户、赫卡提亚的三颗世界球、村纱的船锚及鵺的异形翼尾等辨识特征。
- 根据测试反馈再次重绘小野冢小町，强化其成熟丰满的官方同人常见体型特征，并修复手掌与镰刀刀柄错位，确保刀柄从镰刃接头到画面下缘连续共轴。
- 废弃帽饰错误的上白泽慧音初稿，按参考资料改为紧凑低矮的蓝色学者帽，并保留人形态、史书与寺子屋教师意象。
- 新头像统一使用 `/avatars/<npc.id>.png` 命名，可被 Vue 与旧版兼容头像规则自动命中。

### 高频 NPC 头像扩充（二）
- 新增橙、河城荷取、灵乌路空、火焰猫燐、圣白莲、丰聪耳神子、小野冢小町、四季映姬和多多良小伞九名角色头像。
- 生成前参考东方 Project Wiki 的角色设定与图片，核对发色、服装、帽饰、武器、种族特征和标志性道具。
- 灵乌路空保留右臂控制棒、胸前神眼、黑翼与星空披风；火焰猫燐保留双辫、猫耳和两条火焰猫尾。
- 丰聪耳神子保留角状浅发、紫色耳罩、笏板与光剑；多多良小伞保留异色瞳和独眼长舌唐伞。
- 根据测试反馈重绘丰聪耳神子与小野冢小町：神子改为狐狸耳轮廓的尖锐外展发束，小町恢复成熟高挑、丰满体型，同时保留双马尾珠饰、蓝白服、铜钱腰带与镰刀。
- 新头像使用 `/avatars/<npc.id>.png` 命名，可被 Vue 与旧版兼容头像规则自动命中。

## 2026-06-30

### Vue 游戏界面重构
- 游戏主界面、顶部角色状态、聊天消息、NPC 列表、地图、任务栏和行动输入区迁移为真正的 Vue 组件。
- 新增统一的 Vue 游戏状态桥，旧有世界、对话、任务和存档模块只负责业务数据，不再直接拼接主界面 HTML。
- 聊天消息操作、行动建议、NPC 交互、地图移动、任务追踪与输入提交统一使用 Vue 事件，修复重复绑定造成的重复请求风险。
- 新增独立的 `vue-game.css` 主题层，以 `th-` 命名空间隔离主游戏界面，停止依赖旧样式文件后段覆盖前段的 CSS 链。
- 重建朱红、和纸、金色、湖水青与草木绿组成的东方同人视觉体系，并补齐昼夜主题与手机侧栏布局。
- 保留原有后端 API、角色状态结构和存档数据，旧存档无需手动转换即可进入新版界面。

### 高频 NPC 头像扩充
- 新增十六夜咲夜、射命丸文、魂魄妖梦、藤原妹红、芙兰朵露、红美铃、铃仙、洩矢诹访子和八云蓝九名角色头像。
- 生成前逐一参考东方 Project Wiki 的人物设定与角色图片，核对服装、发色、武器、帽饰、种族特征和标志性道具。
- 芙兰朵露使用左右各四枚、总计八枚水晶的黑色枝状翅膀；魂魄妖梦保留半灵与双刀；八云蓝保留九尾；洩矢诹访子保留双眼青蛙帽。
- 新头像沿用 `/avatars/<npc.id>.png` 自动匹配，无需新增前端分支逻辑。

## 2026-06-29

### 存档加载与行动输入修复
- 统一前端内部 ES 模块导入地址，修复同一状态与 DOM 模块被重复实例化的问题。
- 修复旧存档进入游戏后误报“加载角色失败”以及行动输入始终被判定为空的问题。
- API 错误提示支持解析字符串、字段校验数组和结构化错误，不再显示 `[object Object]`。
- 已用两个现有旧存档完成后端兼容测试，并在浏览器中完成加载、输入与提交回归。

### 顶部工具栏整合
- 将角色简述、当前位置、存活状态、游戏时间、反馈导出和返回操作合并到 Vue 顶部栏。
- 删除游戏面板内重复的昼夜按钮，全局只保留一个主题切换入口。
- 补充窄屏换行布局，顶部信息不会再与游戏主体分离。

### 角色头像
- 新增蓬莱山辉夜原创同人头像，并接入 NPC 头像自动匹配规则。
- 补齐帕秋莉、爱丽丝、古明地觉、古明地恋、秦心、八坂神奈子、八意永琳、风见幽香、西行寺幽幽子和蕾米莉亚原创同人头像。
- 根据角色资料修正古明地恋的额头第三眼错误，保留胸前闭合第三眼；秦心改用格纹上衣、特殊纽扣、灯笼裙与希望之面；风见幽香改为自然撑开的标志性洋伞。
- 帕秋莉的两个历史 NPC ID 共用同一头像；新增风见幽香 `npc_yuuka` 索引并绑定太阳花田，使全部头像可被现有 ID 匹配规则直接命中。

## 2026-06-28

### 正式 SSE 流式生成
- 环境交互与 NPC 对话正式接入 SSE，不再等待完整回复后模拟逐字显示。
- 后端在模型生成结构化 JSON 时实时抽取叙事文本，结束后再发送完整任务、关系、记忆和状态结算。
- 行动按钮在生成期间切换为“停止生成”，支持主动中止请求。
- 流式接口异常时自动回退普通请求，保留原有兼容路径。

### 确定性游戏规则
- 新增后端规则服务，统一裁定符卡胜负、疲劳、伤势、灵力消耗和弹幕熟练度成长。
- 符卡结果由角色能力、熟练度、疲劳、伤势、准备行为和对手层级确定，同一状态与行为可重复得到相同结果。
- 规则预裁定会在生成前注入叙事上下文，AI 不得反转后端胜负。
- 制作人模式保持绝对优势，符卡战固定轻松胜利且不会因规则结算受伤。

### 存档时序与分支
- 角色和任务存档改为临时文件加原子替换，降低异常退出造成 JSON 损坏的风险。
- 对话推进自动创建角色与任务联合快照，每个角色保留最近 40 个节点。
- 主存档损坏时会尝试从最近快照自动恢复。
- 新增时序面板，可覆盖恢复旧节点或创建独立分支角色。

### Vue 功能迁移
- 角色记录列表迁移为 Vue 组件，不再依赖字符串拼接和重复事件绑定。
- 角色继续、删除、导出、转为 NPC、创建、导入和时序管理统一由 Vue 响应式状态控制。
- 新增 Vue 时序弹窗和分支命名流程。
- 修复角色删除请求方法与后端路由不一致的问题。

### 角色与场景素材
- 新增博丽灵梦、雾雨魔理沙、东风谷早苗、八云紫和琪露诺五名高频角色头像。
- 新增红魔馆、雾之湖和守矢神社场景背景。
- 游戏聊天背景会根据当前地点自动切换，未配置地点继续使用博丽神社背景。

### NPC 记忆 RAG
- 新增完全离线的中文字符向量与概念词扩展，不需要额外 embedding API Key。
- 记忆召回升级为本地向量相似度、关键词、重要度、使用次数和时间衰减的混合排序。
- 增加相似记忆去重，避免同类记录占满上下文。
- 保留长期摘要和旧格式记忆兼容。

## 2026-06-27

### 东方同人视觉强化
- 角色入口新增原创博丽神社场景背景，加入鸟居、结界与弹幕纹样，作为实际游戏视觉资产使用。
- 入口页从网页卡片式横幅调整为场景图上的异变标题，强化视觉小说与东方同人作品氛围。
- 移除入口页重复的《东方异变录》标题，改为“幻想乡 / 异变记录”场景导航。
- 角色记录改为御札编号与异变档案式视觉，移动端保持两列稳定操作区。
- 对话气泡新增“行 / 话 / 述 / 告”叙事印记，减少不统一的 Emoji 图标。
- NPC 无立绘时使用按角色名称生成的稳定主题色头像，方便快速区分当前遭遇角色。
- 技术运行标识保留，但仅在制作人模式角色进入游戏后显示；普通玩家界面不再展示 Vue 等技术信息。

### Vue 前端基础架构
- 前端入口升级为 Vue 3，由 Vue 管理应用外壳、启动状态、主题切换、全局加载状态和消息提示。
- Vue 运行时改为本地内置资源，不依赖 CDN，保持测试包与 EXE 离线可用。
- 新增游戏视图挂载桥接，原有角色、对话、NPC、任务和制作人控制台模块可在迁移期间继续使用。
- 游戏面板不再直接占用整个页面根节点，统一挂载到 Vue 游戏视图区域。
- 新增启动失败反馈和重新加载入口，避免模块加载异常时只显示空白页面。

### 界面与适配
- 新增统一应用标题栏、主题控制和前端运行状态标识。
- 重做全局加载遮罩与消息提示，统一成功、错误和普通状态的视觉反馈。
- 优化桌面端与移动端高度、滚动、遮罩层级和窄屏标题栏布局。
- 保留原有后端 API、世界数据和存档结构，旧存档无需转换即可继续使用。

## 2026-06-23

### NPC 长期记忆优化
- 新增后端自动记忆捕获，不再完全依赖 AI 主动返回 `memory_updates`。
- 自动记忆捕获覆盖关系变化、线索推进/完成、符卡战斗、自由探索事件、承诺、约定、救助、背叛、赠礼、秘密、称呼、亲密互动、威胁等关键事件。
- NPC 记忆条目新增 `id`、`importance`、`emotion`、`last_used_at`、`used_count` 等字段。
- 新增记忆重要性评分，综合关键词、来源、标签和显式重要度判断记忆权重。
- NPC 记忆召回从单纯关键词/最近记录升级为“关键词匹配 + 重要度 + 使用次数”的综合排序。
- 被注入 prompt 的记忆会更新召回次数和最后使用时间，便于后续保留高价值记忆。
- 新增 NPC 长期印象摘要 `npc_memory_summaries`，当单个 NPC 记忆过多时自动压缩旧记忆，避免旧内容直接丢失。
- 关系变化会自动写入对应 NPC 记忆，使关系来源长期可追溯。

### 前端与制作人调试
- NPC 详情面板新增“长期印象”显示。
- NPC 详情面板中的近期记忆会显示重要度、情绪和召回次数。
- 制作人控制台新增 NPC 长期记忆管理区。
- 制作人控制台支持新增/改写 NPC 记忆。
- 制作人控制台支持按记忆 ID 删除 NPC 记忆。
- 制作人控制台支持手动压缩指定 NPC 的长期记忆。
- 制作人控制台新增 NPC 记忆预览，显示记忆 ID、内容、重要度和情绪，便于调试。

### 兼容性
- 旧存档自动补齐 `npc_memory_summaries`。
- 旧 NPC 记忆自动补齐记忆 ID、重要度、情绪、召回时间和召回次数。
- 旧格式字符串记忆会自动迁移为结构化记忆对象。

### 待办
- 向量检索/RAG 暂不实施，后续在记忆数据结构稳定后再接入本地向量库或 embedding 检索。

## 2026-06-20

### 测试反馈修复
- 修复打包测试版侧边栏 NPC 不显示的问题。
- 修复打包版首次运行时未正确从 exe 内置资源初始化 `worlds` 数据的问题。
- 世界数据初始化逻辑改为检查关键文件：`worlds_index.json`、地点库、NPC 索引、时间线、世界观文件，缺失时自动补齐。
- NPC 查询接口兼容中文地点名、地点 id、父级区域和旧存档场景名。
- NPC 索引读取改为支持 `utf-8-sig`，避免带 BOM 的 JSON 导致解析失败。

### 启动流程修复
- `启动touhou.bat` 改回双击自动启动游戏服务并打开网页前端。
- 移除 BAT 菜单式启动流程，避免测试者误进入 `.env` 编辑。
- BAT 只负责自动创建空 `.env`、启动 `touhou.exe`、打开 `http://127.0.0.1:8000`。
- API Key 输入流程保持在网页前端弹窗中完成。

### 打包与验证
- 重新构建并替换根目录 `touhou.exe`。
- 重新生成 `release/touhou-test-package.zip`。
- 验证测试包解压后不包含 `worlds` 和旧存档。
- 验证 exe 首次启动后会自动生成世界数据。
- 验证 `博丽神社` 和 `loc_hakurei` 均可返回 5 个 NPC。
- 验证测试包 `.env` 不包含 API Key。

## 2026-06-19

### 游戏体验优化
- 新增旁白与 NPC 对话的渐进式文本显示，改善 AI 回复等待体验。
- 玩家疲劳、伤势、符卡熟练度等状态注入 AI 上下文，使状态影响叙事、行动成功率和战斗表现。
- 回合总结增加状态变化展示，便于测试确认数值系统是否生效。
- 修复玩家同时输入动作和台词后，自己的对话框多出空白行的问题。
- 修复线索推进显示内部编号的问题，改为显示线索名称。

### NPC 与世界内容修复
- 删除重复的橙 NPC，只保留 `npc_chen`。
- 修正博丽灵梦外观描述，改为黑色小皮鞋与白袜。
- 保留后台恋爱/成人内容设定，但前端显示文案避免直白出现成人相关字样。

### 前端与 UI
- 强制网页标题为 `TouHou · 东方异变录`。
- 新增东方主题 favicon，并在页面加载时再次校正标题和图标。
- 新增本地 NPC 头像目录 `avatars`，支持使用 `npc_id.png` 作为头像。
- 头像缺失时自动回退为姓名首字头像。
- 新增版本标识显示。
- 新增“导出反馈”按钮，用于导出测试反馈包。

### 后端与测试工具
- 新增反馈导出接口，可打包角色快照、最近对话、任务、关系、事件和符卡记录。
- 修复 `ghost.py` 中缺少 `re` 导入的潜在运行错误。
- NPC、角色、地点相关数据读取增强兼容性。

### 打包
- 更新 PyInstaller 打包清单，加入 `avatars` 目录。
- 重新构建根目录 `touhou.exe`。
- 重新生成干净测试包。
- 测试包不包含旧存档，不包含开发者 API Key。

## 2026-06-18

### 项目命名与主题统一
- 将项目中面向玩家的旧品牌名称统一替换为 `touhou` / `TouHou`。
- 网页标题由旧名称调整为 `TouHou · 东方异变录`。
- 游戏标题从《幻想乡异变录》调整为《东方异变录》。
- 前端视觉风格向东方 Project 同人主题靠拢。

### 单世界化
- 删除或隐藏其他世界入口，只保留 TouHou / 东方世界。
- 默认世界固定为 `world_touhou`。
- 创建角色与进入游戏流程围绕东方世界重整。

### 创建角色优化
- 新增和调整多个创建模板。
- 增加完全自定义模板。
- 增加游戏制作人专用模板。
- 游戏制作人专用模板能力设定为最高级，便于测试和调试。
- 隐藏游戏制作人模式入口提示，不在普通创建角色流程中展示触发指令。

### 游戏系统优化
- 增加制作人模式专属控制台。
- 增加玩家状态、疲劳、符卡熟练度等可追踪属性。
- 符卡战斗后可影响熟练度或状态变化。
- 异变完成后倒计时状态调整为等待后续异变，避免已完成任务仍显示扩散倒计时。
- 缘分变化提示改为只显示本轮变动项。
- 东风谷早苗位置调整到守矢神社相关区域。

### API Key 与分发
- 改为首次启动由测试者自行填写 API Key。
- `.env` 中清除已有具体 API Key。
- 打包测试版时避免带入开发者 API Key。
- 打包测试版时避免带入已有存档。
- 生成可直接分发测试的 zip 包。

### 兼容性
- 旧存档保留兼容与自动升级逻辑。
- 新增字段缺失时自动补齐，降低旧存档进入新版后的报错风险。
