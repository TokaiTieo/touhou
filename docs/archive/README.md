# Historical Helpers

`make_docx_2026-08-19.py` was moved from `.tmp/make_docx.py` without changing its contents.
It generated the existing August 19 technical-discussion document and is not used by the game,
tests, or release build. Run it only deliberately from the project root: it overwrites its
named document output. The existing user document has not been modified.
